# Supabase — copie para .env.local e preencha com as credenciais do projeto
# NUNCA versione o .env.local no Git (já está no .gitignore)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# WhatsApp / lead routing (preencher quando definido com o cliente)
VITE_WHATSAPP_NUMERO_PADRAO=
